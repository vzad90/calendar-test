{
  "name": "calendar-test-task",
  "version": "1.0.0",
  "description": "Calendar full-stack app — Express API + React frontend",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "lint": "npm run lint --prefix server && npm run lint --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
